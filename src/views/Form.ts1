/// reference "../docs.ts"

class FormView extends Docs.View<any> {
    renderJson(data: any): string {
        // var children: Node[] = [];
        // var src = <HTMLElement>source;
        // for (var i = 0; i < src.childNodes.length; i++) {
        //     children.push(src.childNodes[i]);
        // }
        // return children;
        return "todo";
    }
}

Docs.views["form"] = new FormView();